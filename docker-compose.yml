version: "3.3"

services:
  redis:
    image: "redis"
    restart: on-failure
  mysql:
    image: "mysql"
    restart: on-failure
    env_file:
      - ./.env.docker
    volumes:
      - mysql-data:/var/lib/mysql
  server:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - ./.env.docker
    restart: on-failure
    depends_on:
      - "redis"
      - "mysql"

volumes:
  mysql-data:
